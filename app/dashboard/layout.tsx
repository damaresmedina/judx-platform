export const metadata = {
  title: 'Dashboard — JudX',
  description: 'Painel de inteligencia jurisprudencial do STF e STJ.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
